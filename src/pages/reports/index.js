import { Layout } from '../../shared/components';

const Reports = () => {
  return (
    <Layout
      headTitle="Reports"
    >
      <div>Hi from Reports</div>
    </Layout>
  );
};

export default Reports;
